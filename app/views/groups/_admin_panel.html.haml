= link_to "add article", "#", class: "btn btn-default btn-xs", data: { toggle: "modal", target: "#add_article_to_group" }
= link_to "delete group", group, method: :delete,  class: "btn btn-danger btn-xs", "data-confirm" => "Really want to delete this group?"
/ .well
/   .row
/     .col-md-6 
/       %h3 Views
/       %ul.list-group
/         - group.views.each do |view|
/           %li.list-group-item
/             %h4
/               = "#{time_ago_in_words(view.created_at)} #{t('ago')}. on"
/               .label.label-success #{view.article.theme}
/     .col-md-6
/       %h3 Comments
/       %ul.list-group
/         - group.comments.each do |comment|
/           %li.list-group-item
/             %h4
/               = "#{time_ago_in_words(comment.created_at)} #{t('ago')}. on"
/               .label.label-success #{comment.article.theme}