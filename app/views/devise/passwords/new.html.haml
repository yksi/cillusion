%h2  
  Forgot your password?

= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
  .row  
    .col-md-8= f.email_field :email, :autofocus => true, class: 'form-control', placeholder: 'Your email'

    .col-md-4= f.submit "Send me reset password instructions", class: 'btn btn-primary'
